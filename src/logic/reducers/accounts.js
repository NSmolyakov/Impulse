let initialState = {
    Accounts:[
        {id:0, name:'Николай Смоляков', status:'Онлайн', login:'root', password:'1234'},
    ]
}


const accountsReducer = (state = initialState) => {
    return(state)
}

export default accountsReducer;